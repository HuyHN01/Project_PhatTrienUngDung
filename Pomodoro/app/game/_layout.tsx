import { Stack } from 'expo-router';

export default function GameStackLayout() {
  return <Stack screenOptions={{ headerTitleAlign: 'center', headerTintColor: 'white', headerStyle: { backgroundColor: '#1A202C' } }} />;
}
